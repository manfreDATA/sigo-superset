# .env-local.example
# Copy this file to superset/docker/.env-local and fill in values.
# This file is loaded by docker-compose-non-dev.yml (env_file: ./docker/.env-local).
# Keep secrets out of version control; store real secrets in a secure vault or CI secrets.

# Postgres (DB) settings used by the db service and available to Superset containers
POSTGRES_USER=superset
POSTGRES_PASSWORD=superset
POSTGRES_DB=superset
# Optional: if you want to use an external DB, set SQLALCHEMY_DATABASE_URI instead of relying on the docker db
# SQLALCHEMY_DATABASE_URI=postgresql+psycopg2://<user>:<pass>@<host>:<port>/<db>

# Superset image name: compose will build from ./docker and tag this image
SUPERSET_IMAGE=sigo-superset:latest

# Exposed port for Superset web UI
SUPERSET_PORT=8088

# Superset environment flags
SUPERSET_ENV=production
FLASK_ENV=production

# Superset admin initial user (used by superset-init service to create admin; optional but handy)
SUPERSET_ADMIN_USERNAME=admin
SUPERSET_ADMIN_FIRSTNAME=Admin
SUPERSET_ADMIN_LASTNAME=User
SUPERSET_ADMIN_EMAIL=admin@example.com
SUPERSET_ADMIN_PASSWORD=admin_password_here

# Optional secret key for Flask sessions and Superset
SECRET_KEY=replace_with_a_random_secret_key

# Redis/Celery (defaults assume local redis service in this compose)
CELERY_BROKER_URL=redis://redis:6379/0
CELERY_RESULT_BACKEND=redis://redis:6379/1
# If you use a redis password:
# REDIS_PASSWORD=
# Example broker with password: redis://:${REDIS_PASSWORD}@redis:6379/0

# Optional: S3 or local storage settings for uploads and charts (update superset_config_docker.py accordingly)
# RESULTS_BACKEND_TYPE=redis
# RESULTS_BACKEND_HOST=redis
# RESULTS_BACKEND_PORT=6379

# Optional mapping / 3rd party keys used by dashboards (replace with your keys)
MAPBOX_API_KEY=

# Additional environment variables you may want to expose to Superset:
# - any env vars read by your superset_config_docker.py (database URIs, OAuth client IDs/secrets, etc.)
# Example:
# OAUTH_PROVIDER_CLIENT_ID=...
# OAUTH_PROVIDER_CLIENT_SECRET=...

# NOTE: remove this comment block and commit only the file name, not secrets, or keep a .env-local.example in repo and
# keep the filled superset/docker/.env-local out of version control (.gitignore).